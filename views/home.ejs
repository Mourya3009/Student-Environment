<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>WebCorp - About Us</title>
    <style>
        :root {
            /* Light mode colors */
            --bg-primary: #F8F9FA;
            --bg-secondary: rgba(255, 255, 255, 0.9);
            --bg-card: rgba(255, 255, 255, 0.9);
            --text-primary: #2E2E2E;
            --text-secondary: rgba(46, 46, 46, 0.8);
            --border-color: rgba(92, 107, 192, 0.1);
            --shadow-color: rgba(92, 107, 192, 0.1);
            --shadow-hover: rgba(92, 107, 192, 0.2);
            
            /* Theme colors (consistent) */
            --primary: #5C6BC0;
            --accent: #F48FB1;
            --secondary-accent: #A5D6A7;
            --hover: #9575CD;
        }

        [data-theme="dark"] {
            /* Dark mode colors */
            --bg-primary: #0f0f0f;
            --bg-secondary: rgba(30, 30, 30, 0.95);
            --bg-card: rgba(40, 40, 40, 0.9);
            --text-primary: #ffffff;
            --text-secondary: rgba(255, 255, 255, 0.8);
            --border-color: rgba(92, 107, 192, 0.3);
            --shadow-color: rgba(0, 0, 0, 0.3);
            --shadow-hover: rgba(0, 0, 0, 0.5);
        }

        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }

        body {
            font-family: -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, sans-serif;
            line-height: 1.6;
            color: var(--text-primary);
            background: var(--bg-primary);
            min-height: 100vh;
            overflow-x: hidden;
            transition: background-color 0.3s ease, color 0.3s ease;
        }

        /* Animated background elements */
        .bg-decoration {
            position: fixed;
            top: 0;
            left: 0;
            width: 100%;
            height: 100%;
            pointer-events: none;
            z-index: -1;
        }

        .bg-decoration::before {
            content: '';
            position: absolute;
            top: -50%;
            right: -20%;
            width: 800px;
            height: 800px;
            background: radial-gradient(circle, rgba(92, 107, 192, 0.05) 0%, transparent 70%);
            border-radius: 50%;
            animation: float 20s ease-in-out infinite;
        }

        .bg-decoration::after {
            content: '';
            position: absolute;
            bottom: -30%;
            left: -10%;
            width: 600px;
            height: 600px;
            background: radial-gradient(circle, rgba(244, 143, 177, 0.05) 0%, transparent 70%);
            border-radius: 50%;
            animation: float 25s ease-in-out infinite reverse;
        }

        [data-theme="dark"] .bg-decoration::before {
            background: radial-gradient(circle, rgba(92, 107, 192, 0.1) 0%, transparent 70%);
        }

        [data-theme="dark"] .bg-decoration::after {
            background: radial-gradient(circle, rgba(244, 143, 177, 0.1) 0%, transparent 70%);
        }

        @keyframes float {
            0%, 100% { transform: translateY(0px) rotate(0deg); }
            50% { transform: translateY(-20px) rotate(5deg); }
        }

        .container {
            max-width: 1200px;
            margin: 0 auto;
            padding: 0 1rem;
        }

        /* Dark Mode Toggle */
        .theme-toggle {
            position: relative;
            width: 60px;
            height: 30px;
            background: var(--border-color);
            border-radius: 50px;
            cursor: pointer;
            transition: all 0.3s ease;
            border: 2px solid var(--primary);
            display: flex;
            align-items: center;
            padding: 2px;
        }

        .theme-toggle:hover {
            background: var(--primary);
        }

        .theme-toggle-slider {
            width: 22px;
            height: 22px;
            background: linear-gradient(135deg, var(--primary) 0%, var(--accent) 100%);
            border-radius: 50%;
            transition: all 0.3s cubic-bezier(0.4, 0, 0.2, 1);
            display: flex;
            align-items: center;
            justify-content: center;
            box-shadow: 0 2px 8px rgba(0, 0, 0, 0.2);
            position: relative;
        }

        .theme-toggle-slider svg {
            width: 12px;
            height: 12px;
            color: white;
            transition: all 0.3s ease;
        }

        [data-theme="dark"] .theme-toggle-slider {
            transform: translateX(30px);
        }

        .theme-toggle-slider .sun-icon {
            opacity: 1;
            transform: rotate(0deg);
        }

        .theme-toggle-slider .moon-icon {
            opacity: 0;
            transform: rotate(180deg);
            position: absolute;
        }

        [data-theme="dark"] .theme-toggle-slider .sun-icon {
            opacity: 0;
            transform: rotate(180deg);
        }

        [data-theme="dark"] .theme-toggle-slider .moon-icon {
            opacity: 1;
            transform: rotate(0deg);
        }

        /* Header Styles */
        .header {
            background: var(--bg-secondary);
            backdrop-filter: blur(20px);
            border-bottom: 1px solid var(--border-color);
            position: sticky;
            top: 0;
            z-index: 50;
            transition: all 0.3s ease;
        }

        .header-content {
            display: flex;
            align-items: center;
            justify-content: space-between;
            padding: 1.2rem 0;
        }

        .header-right {
            display: flex;
            align-items: center;
            gap: 1.5rem;
        }

        .logo {
            display: flex;
            align-items: center;
            gap: 0.75rem;
            transition: transform 0.3s ease;
        }

        .logo:hover {
            transform: scale(1.05);
        }

        .logo-icon {
            width: 2.8rem;
            height: 2.8rem;
            background: linear-gradient(135deg, var(--primary) 0%, var(--accent) 100%);
            border-radius: 0.75rem;
            display: flex;
            align-items: center;
            justify-content: center;
            color: white;
            font-weight: bold;
            font-size: 1.25rem;
            box-shadow: 0 4px 15px rgba(92, 107, 192, 0.3);
            position: relative;
            overflow: hidden;
        }

        .logo-icon::before {
            content: '';
            position: absolute;
            top: -50%;
            left: -50%;
            width: 200%;
            height: 200%;
            background: linear-gradient(45deg, transparent, rgba(255, 255, 255, 0.3), transparent);
            transform: rotate(45deg);
            transition: all 0.6s;
            opacity: 0;
        }

        .logo:hover .logo-icon::before {
            animation: shine 0.6s ease-in-out;
        }

        @keyframes shine {
            0% { transform: translateX(-100%) translateY(-100%) rotate(45deg); opacity: 0; }
            50% { opacity: 1; }
            100% { transform: translateX(100%) translateY(100%) rotate(45deg); opacity: 0; }
        }

        .logo-text {
            font-size: 1.4rem;
            font-weight: 700;
            color: var(--text-primary);
            background: linear-gradient(135deg, var(--primary), var(--accent));
            -webkit-background-clip: text;
            -webkit-text-fill-color: transparent;
            background-clip: text;
        }

        .auth-buttons {
            display: flex;
            align-items: center;
            gap: 1rem;
        }

        .btn {
            padding: 0.7rem 1.5rem;
            border-radius: 50px;
            text-decoration: none;
            font-weight: 600;
            transition: all 0.3s cubic-bezier(0.4, 0, 0.2, 1);
            border: none;
            cursor: pointer;
            display: inline-block;
            text-align: center;
            position: relative;
            overflow: hidden;
        }

        .btn::before {
            content: '';
            position: absolute;
            top: 0;
            left: -100%;
            width: 100%;
            height: 100%;
            background: linear-gradient(90deg, transparent, rgba(255, 255, 255, 0.2), transparent);
            transition: left 0.5s;
        }

        .btn:hover::before {
            left: 100%;
        }

        .btn-outline {
            background: var(--bg-card);
            color: var(--primary);
            border: 2px solid var(--primary);
            backdrop-filter: blur(10px);
        }

        .btn-outline:hover {
            background: var(--primary);
            color: white;
            transform: translateY(-2px);
            box-shadow: 0 8px 25px rgba(92, 107, 192, 0.3);
        }

        .btn-primary {
            background: linear-gradient(135deg, var(--primary) 0%, var(--hover) 100%);
            color: white;
            box-shadow: 0 4px 15px rgba(92, 107, 192, 0.3);
        }

        .btn-primary:hover {
            transform: translateY(-2px);
            box-shadow: 0 8px 25px rgba(149, 117, 205, 0.4);
        }

        .btn-secondary {
            background: var(--bg-card);
            color: var(--primary);
            backdrop-filter: blur(10px);
        }

        .btn-secondary:hover {
            background: white;
            color: var(--hover);
            transform: translateY(-2px);
        }

        .btn-lg {
            padding: 1rem 2rem;
            font-size: 1.125rem;
        }

        /* Hero Section */
        .hero {
            text-align: center;
            margin: 4rem 0 5rem 0;
            position: relative;
        }

        .hero-logo {
            width: 8rem;
            height: 8rem;
            background: linear-gradient(135deg, var(--primary) 0%, var(--accent) 100%);
            border-radius: 2rem;
            display: flex;
            align-items: center;
            justify-content: center;
            margin: 0 auto 3rem auto;
            color: white;
            font-weight: bold;
            font-size: 3rem;
            box-shadow: 0 20px 40px rgba(92, 107, 192, 0.3);
            position: relative;
            animation: heroFloat 6s ease-in-out infinite;
        }

        @keyframes heroFloat {
            0%, 100% { transform: translateY(0px) rotate(0deg); }
            50% { transform: translateY(-10px) rotate(2deg); }
        }

        .hero-logo::after {
            content: '';
            position: absolute;
            top: -20px;
            left: -20px;
            right: -20px;
            bottom: -20px;
            background: linear-gradient(135deg, rgba(92, 107, 192, 0.2), rgba(244, 143, 177, 0.2));
            border-radius: 2.5rem;
            z-index: -1;
            animation: pulse 4s ease-in-out infinite;
        }

        @keyframes pulse {
            0%, 100% { transform: scale(1); opacity: 0.7; }
            50% { transform: scale(1.05); opacity: 0.3; }
        }

        .hero h1 {
            font-size: clamp(3rem, 6vw, 5rem);
            font-weight: 800;
            background: linear-gradient(135deg, var(--text-primary) 0%, var(--primary) 50%, var(--accent) 100%);
            -webkit-background-clip: text;
            -webkit-text-fill-color: transparent;
            background-clip: text;
            margin-bottom: 2rem;
            animation: fadeInUp 1s ease-out;
        }

        .hero p {
            font-size: 1.4rem;
            color: var(--text-secondary);
            max-width: 50rem;
            margin: 0 auto;
            animation: fadeInUp 1s ease-out 0.2s both;
        }

        @keyframes fadeInUp {
            from {
                opacity: 0;
                transform: translateY(30px);
            }
            to {
                opacity: 1;
                transform: translateY(0);
            }
        }

        /* Card Styles */
        .card {
            background: var(--bg-card);
            border-radius: 1.5rem;
            box-shadow: 0 8px 32px var(--shadow-color);
            transition: all 0.4s cubic-bezier(0.4, 0, 0.2, 1);
            border: 1px solid var(--border-color);
            backdrop-filter: blur(10px);
            position: relative;
            overflow: hidden;
        }

        .card::before {
            content: '';
            position: absolute;
            top: 0;
            left: 0;
            right: 0;
            height: 3px;
            background: linear-gradient(90deg, var(--primary), var(--accent), var(--secondary-accent));
            opacity: 0;
            transition: opacity 0.3s ease;
        }

        .card:hover {
            transform: translateY(-8px);
            box-shadow: 0 20px 40px var(--shadow-hover);
        }

        .card:hover::before {
            opacity: 1;
        }

        .card-content {
            padding: 2.5rem;
        }

        .card-content-lg {
            padding: 3rem;
        }

        /* Grid Layouts */
        .grid {
            display: grid;
            gap: 2rem;
        }

        .grid-cols-2 {
            grid-template-columns: repeat(2, 1fr);
        }

        .grid-cols-3 {
            grid-template-columns: repeat(3, 1fr);
        }

        @media (max-width: 768px) {
            .grid-cols-2,
            .grid-cols-3 {
                grid-template-columns: 1fr;
            }
        }

        @media (min-width: 768px) and (max-width: 1024px) {
            .grid-cols-3 {
                grid-template-columns: repeat(2, 1fr);
            }
        }

        /* Content Sections */
        .content {
            max-width: 70rem;
            margin: 0 auto;
        }

        .section {
            margin-bottom: 4rem;
            animation: fadeInUp 0.8s ease-out;
        }

        .section h2 {
            font-size: 2.5rem;
            font-weight: 800;
            background: linear-gradient(135deg, var(--text-primary) 0%, var(--primary) 100%);
            -webkit-background-clip: text;
            -webkit-text-fill-color: transparent;
            background-clip: text;
            margin-bottom: 2rem;
            text-align: center;
            position: relative;
        }

        .section h2::after {
            content: '';
            position: absolute;
            bottom: -10px;
            left: 50%;
            transform: translateX(-50%);
            width: 60px;
            height: 4px;
            background: linear-gradient(90deg, var(--primary), var(--accent));
            border-radius: 2px;
        }

        .section h3 {
            font-size: 1.4rem;
            font-weight: 700;
            margin-bottom: 1rem;
            color: var(--text-primary);
        }

        .text-lg {
            font-size: 1.2rem;
            line-height: 1.8;
            color: var(--text-secondary);
        }

        .text-content {
            margin-bottom: 1.5rem;
        }

        /* Service Cards */
        .service-icon {
            width: 4rem;
            height: 4rem;
            margin-bottom: 1.5rem;
            padding: 1rem;
            border-radius: 1rem;
            transition: all 0.3s ease;
        }

        .icon-primary { 
            color: var(--primary); 
            background: rgba(92, 107, 192, 0.1);
        }
        .icon-accent { 
            color: var(--accent); 
            background: rgba(244, 143, 177, 0.1);
        }
        .icon-secondary { 
            color: var(--secondary-accent); 
            background: rgba(165, 214, 167, 0.1);
        }

        .card:hover .service-icon {
            transform: scale(1.1) rotate(5deg);
        }

        /* Feature List */
        .feature-list {
            display: grid;
            grid-template-columns: repeat(2, 1fr);
            gap: 3rem;
        }

        @media (max-width: 768px) {
            .feature-list {
                grid-template-columns: 1fr;
            }
        }

        .feature-item {
            display: flex;
            align-items: flex-start;
            gap: 1.5rem;
            padding: 1.5rem;
            border-radius: 1rem;
            transition: all 0.3s ease;
        }

        .feature-item:hover {
            background: rgba(165, 214, 167, 0.05);
            transform: translateX(10px);
        }

        [data-theme="dark"] .feature-item:hover {
            background: rgba(165, 214, 167, 0.1);
        }

        .feature-icon {
            width: 2rem;
            height: 2rem;
            color: var(--secondary-accent);
            margin-top: 0.25rem;
            flex-shrink: 0;
            padding: 0.5rem;
            background: rgba(165, 214, 167, 0.1);
            border-radius: 0.5rem;
        }

        /* CTA Section */
        .cta-card {
            background: linear-gradient(135deg, var(--primary) 0%, var(--accent) 100%);
            color: white;
            position: relative;
            overflow: hidden;
        }

        .cta-card::before {
            content: '';
            position: absolute;
            top: -50%;
            left: -50%;
            width: 200%;
            height: 200%;
            background: radial-gradient(circle, rgba(255, 255, 255, 0.1) 0%, transparent 70%);
            animation: rotate 20s linear infinite;
        }

        @keyframes rotate {
            from { transform: rotate(0deg); }
            to { transform: rotate(360deg); }
        }

        .cta-buttons {
            display: flex;
            flex-direction: column;
            gap: 1.5rem;
            justify-content: center;
            align-items: center;
        }

        @media (min-width: 640px) {
            .cta-buttons {
                flex-direction: row;
            }
        }

        .btn-outline-white {
            background: rgba(255, 255, 255, 0.1);
            color: white;
            border: 2px solid rgba(255, 255, 255, 0.3);
            backdrop-filter: blur(10px);
        }

        .btn-outline-white:hover {
            background: rgba(255, 255, 255, 0.9);
            color: var(--primary);
            border-color: white;
        }

        /* Footer */
        .footer {
            background: linear-gradient(135deg, var(--text-primary) 0%, #1a1a1a 100%);
            color: white;
            padding: 4rem 0;
            margin-top: 5rem;
            position: relative;
        }

        [data-theme="dark"] .footer {
            background: linear-gradient(135deg, #000000 0%, #1a1a1a 100%);
        }

        .footer::before {
            content: '';
            position: absolute;
            top: 0;
            left: 0;
            right: 0;
            height: 1px;
            background: linear-gradient(90deg, transparent, var(--primary), var(--accent), var(--secondary-accent), transparent);
        }

        .footer-content {
            display: flex;
            flex-direction: column;
            align-items: center;
            justify-content: space-between;
            margin-bottom: 2rem;
        }

        @media (min-width: 768px) {
            .footer-content {
                flex-direction: row;
            }
        }

        .footer-logo {
            display: flex;
            align-items: center;
            gap: 1rem;
            margin-bottom: 1rem;
        }

        @media (min-width: 768px) {
            .footer-logo {
                margin-bottom: 0;
            }
        }

        .footer-logo-icon {
            width: 2.5rem;
            height: 2.5rem;
            background: linear-gradient(135deg, var(--primary) 0%, var(--accent) 100%);
            border-radius: 0.75rem;
            display: flex;
            align-items: center;
            justify-content: center;
            color: white;
            font-weight: bold;
            box-shadow: 0 4px 15px rgba(92, 107, 192, 0.3);
        }

        .footer-links {
            display: flex;
            align-items: center;
            gap: 2rem;
        }

        .footer-links a {
            color: var(--secondary-accent);
            text-decoration: none;
            transition: all 0.3s ease;
            position: relative;
        }

        .footer-links a::after {
            content: '';
            position: absolute;
            bottom: -5px;
            left: 0;
            width: 0;
            height: 2px;
            background: var(--accent);
            transition: width 0.3s ease;
        }

        .footer-links a:hover {
            color: var(--accent);
            transform: translateY(-2px);
        }

        .footer-links a:hover::after {
            width: 100%;
        }

        .footer-bottom {
            border-top: 1px solid rgba(92, 107, 192, 0.3);
            padding-top: 2rem;
            text-align: center;
            color: rgba(255, 255, 255, 0.7);
        }

        /* Scroll animations */
        .scroll-reveal {
            opacity: 0;
            transform: translateY(50px);
            transition: all 0.8s ease;
        }

        .scroll-reveal.revealed {
            opacity: 1;
            transform: translateY(0);
        }

        /* Responsive adjustments */
        @media (max-width: 640px) {
            .auth-buttons {
                flex-direction: column;
                gap: 0.75rem;
            }

            .header-right {
                flex-direction: column;
                gap: 1rem;
            }

            .hero h1 {
                font-size: 2.5rem;
            }

            .hero-logo {
                width: 6rem;
                height: 6rem;
                font-size: 2rem;
            }

            .card-content,
            .card-content-lg {
                padding: 2rem;
            }

            .section h2 {
                font-size: 2rem;
            }
        }
    </style>
</head>
<body>
    <div class="bg-decoration"></div>

    <!-- Header -->
    <header class="header">
        <div class="container">
            <div class="header-content">
                <div class="logo">
                    <div class="logo-icon">W</div>
                    <span class="logo-text">WebCorp</span>
                </div>
                <div class="header-right">
                    <div class="theme-toggle" onclick="toggleTheme()">
                        <div class="theme-toggle-slider">
                            <svg class="sun-icon" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                                <circle cx="12" cy="12" r="5"></circle>
                                <line x1="12" y1="1" x2="12" y2="3"></line>
                                <line x1="12" y1="21" x2="12" y2="23"></line>
                                <line x1="4.22" y1="4.22" x2="5.64" y2="5.64"></line>
                                <line x1="18.36" y1="18.36" x2="19.78" y2="19.78"></line>
                                <line x1="1" y1="12" x2="3" y2="12"></line>
                                <line x1="21" y1="12" x2="23" y2="12"></line>
                                <line x1="4.22" y1="19.78" x2="5.64" y2="18.36"></line>
                                <line x1="18.36" y1="5.64" x2="19.78" y2="4.22"></line>
                            </svg>
                            <svg class="moon-icon" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                                <path d="M21 12.79A9 9 0 1 1 11.21 3 7 7 0 0 0 21 12.79z"></path>
                            </svg>
                        </div>
                    </div>
                    <div class="auth-buttons">
                        <a href="/login" class="btn btn-primary">Login/Sign Up</a>
                    </div>
                </div>
            </div>
        </div>
    </header>

    <!-- Main Content -->
    <main class="container">
        <!-- Hero Section -->
        <section class="hero">
            <div class="hero-logo">W</div>
            <h1>Welcome to WebCorp</h1>
            <p>Transforming businesses through innovative digital solutions and cutting-edge technology</p>
        </section>

        <!-- Content -->
        <div class="content">
            <!-- Mission Statement -->
            <section class="section scroll-reveal">
                <div class="card">
                    <div class="card-content-lg">
                        <h2>Our Mission</h2>
                        <div class="text-content">
                            <p class="text-lg">
                                At WebCorp, we believe that every business deserves access to world-class digital solutions. Founded in
                                2020, we've been at the forefront of digital transformation, helping companies of all sizes leverage
                                technology to achieve their goals and exceed their expectations.
                            </p>
                        </div>
                        <p class="text-lg">
                            Our mission is simple yet powerful: to democratize access to premium digital services while maintaining
                            the highest standards of quality, security, and innovation. We're not just service providers; we're your
                            strategic partners in digital success.
                        </p>
                    </div>
                </div>
            </section>

            <!-- What We Do -->
            <section class="section scroll-reveal">
                <h2>What We Do</h2>
                <div class="grid grid-cols-3">
                    <div class="card">
                        <div class="card-content">
                            <svg class="service-icon icon-primary" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M13 10V3L4 14h7v7l9-11h-7z"></path>
                            </svg>
                            <h3>Web Development</h3>
                            <p>Custom web applications built with modern frameworks and technologies. From simple websites to complex enterprise solutions.</p>
                        </div>
                    </div>

                    <div class="card">
                        <div class="card-content">
                            <svg class="service-icon icon-accent" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M12 4.354a4 4 0 110 5.292M15 21H3v-1a6 6 0 0112 0v1zm0 0h6v-1a6 6 0 00-9-5.197m13.5-9a2.5 2.5 0 11-5 0 2.5 2.5 0 015 0z"></path>
                            </svg>
                            <h3>Digital Strategy</h3>
                            <p>Comprehensive digital transformation strategies tailored to your business needs and market requirements.</p>
                        </div>
                    </div>

                    <div class="card">
                        <div class="card-content">
                            <svg class="service-icon icon-secondary" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9 12l2 2 4-4m5.618-4.016A11.955 11.955 0 0112 2.944a11.955 11.955 0 01-8.618 3.04A12.02 12.02 0 003 9c0 5.591 3.824 10.29 9 11.622 5.176-1.332 9-6.03 9-11.622 0-1.042-.133-2.052-.382-3.016z"></path>
                            </svg>
                            <h3>Security Solutions</h3>
                            <p>Enterprise-grade security implementations to protect your digital assets and customer data.</p>
                        </div>
                    </div>
                </div>
            </section>

            <!-- Our Story -->
            <section class="section scroll-reveal">
                <div class="card">
                    <div class="card-content-lg">
                        <h2>Our Story</h2>
                        <div class="text-content">
                            <p class="text-lg">
                                WebCorp was born from a simple observation: too many businesses were struggling with outdated digital
                                infrastructure and complex technology solutions that didn't serve their actual needs. Our founders,
                                with over 20 years of combined experience in enterprise software and digital transformation, decided
                                to create something different.
                            </p>
                        </div>
                        <div class="text-content">
                            <p class="text-lg">
                                We started with a small team of passionate developers and designers who shared a common vision:
                                technology should empower businesses, not complicate them. From our first client—a local restaurant
                                looking to modernize their ordering system—to our current portfolio of Fortune 500 companies, we've
                                maintained the same core principles.
                            </p>
                        </div>
                        <p class="text-lg">
                            Today, WebCorp serves over 500 clients across 15 countries, with a team of 50+ dedicated
                            professionals. We've processed over $2 billion in transactions through our platforms and helped our
                            clients achieve an average of 40% improvement in operational efficiency.
                        </p>
                    </div>
                </div>
            </section>

            <!-- Why Choose Us -->
            <section class="section scroll-reveal">
                <h2>Why Choose WebCorp?</h2>
                <div class="feature-list">
                    <div>
                        <div class="feature-item">
                            <svg class="feature-icon" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9 12l2 2 4-4m6 2a9 9 0 11-18 0 9 9 0 0118 0z"></path>
                            </svg>
                            <div>
                                <h3>Proven Track Record</h3>
                                <p>Over 500 successful projects delivered on time and within budget, with a 98% client satisfaction rate.</p>
                            </div>
                        </div>

                        <div class="feature-item">
                            <svg class="feature-icon" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9 12l2 2 4-4m6 2a9 9 0 11-18 0 9 9 0 0118 0z"></path>
                            </svg>
                            <div>
                                <h3>Expert Team</h3>
                                <p>Certified professionals with expertise in the latest technologies and industry best practices.</p>
                            </div>
                        </div>

                        <div class="feature-item">
                            <svg class="feature-icon" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9 12l2 2 4-4m6 2a9 9 0 11-18 0 9 9 0 0118 0z"></path>
                            </svg>
                            <div>
                                <h3>24/7 Support</h3>
                                <p>Round-the-clock technical support and maintenance to ensure your systems run smoothly.</p>
                            </div>
                        </div>
                    </div>

                    <div>
                        <div class="feature-item">
                            <svg class="feature-icon" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9 12l2 2 4-4m6 2a9 9 0 11-18 0 9 9 0 0118 0z"></path>
                            </svg>
                            <div>
                                <h3>Scalable Solutions</h3>
                                <p>Future-proof architectures that grow with your business, from startup to enterprise scale.</p>
                            </div>
                        </div>

                        <div class="feature-item">
                            <svg class="feature-icon" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9 12l2 2 4-4m6 2a9 9 0 11-18 0 9 9 0 0118 0z"></path>
                            </svg>
                            <div>
                                <h3>Transparent Pricing</h3>
                                <p>No hidden fees or surprise costs. Clear, upfront pricing with detailed project breakdowns.</p>
                            </div>
                        </div>

                        <div class="feature-item">
                            <svg class="feature-icon" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9 12l2 2 4-4m6 2a9 9 0 11-18 0 9 9 0 0118 0z"></path>
                            </svg>
                            <div>
                                <h3>Innovation Focus</h3>
                                <p>Continuous investment in R&D to bring you the latest technological innovations and competitive advantages.</p>
                            </div>
                        </div>
                    </div>
                </div>
            </section>

            <!-- Call to Action -->
            <section class="section scroll-reveal">
                <div class="card cta-card">
                    <div class="card-content-lg" style="text-align: center; position: relative; z-index: 1;">
                        <h2 style="color: white; margin-bottom: 1rem;">Ready to Transform Your Business?</h2>
                        <p style="font-size: 1.25rem; margin-bottom: 2rem; opacity: 0.9;">
                            Join hundreds of successful companies who trust WebCorp with their digital future.
                        </p>
                        <div class="cta-buttons">
                            <a href="/signup" class="btn btn-secondary btn-lg">Get Started Today</a>
                            <a href="/contact" class="btn btn-outline-white btn-lg">Schedule a Consultation</a>
                        </div>
                    </div>
                </div>
            </section>
        </div>
    </main>

    <!-- Footer -->
    <footer class="footer">
        <div class="container">
            <div class="footer-content">
                <div class="footer-logo">
                    <div class="footer-logo-icon">W</div>
                    <span style="font-size: 1.125rem; font-weight: 600;">WebCorp</span>
                </div>
                <div class="footer-links">
                    <a href="/privacy">Privacy Policy</a>
                    <a href="/terms">Terms of Service</a>
                    <a href="/contact">Contact Us</a>
                </div>
            </div>
            <div class="footer-bottom">
                <p>&copy; 2024 WebCorp. All rights reserved.</p>
            </div>
        </div>
    </footer>

    <script>
        // Theme toggle functionality
        function toggleTheme() {
            const currentTheme = document.documentElement.getAttribute('data-theme');
            const newTheme = currentTheme === 'dark' ? 'light' : 'dark';
            
            document.documentElement.setAttribute('data-theme', newTheme);
            localStorage.setItem('theme', newTheme);
        }

        // Load saved theme on page load
        function loadTheme() {
            const savedTheme = localStorage.getItem('theme');
            const prefersDark = window.matchMedia('(prefers-color-scheme: dark)').matches;
            const theme = savedTheme || (prefersDark ? 'dark' : 'light');
            
            document.documentElement.setAttribute('data-theme', theme);
        }

        // Initialize theme
        loadTheme();

        // Listen for system theme changes
        window.matchMedia('(prefers-color-scheme: dark)').addEventListener('change', (e) => {
            if (!localStorage.getItem('theme')) {
                document.documentElement.setAttribute('data-theme', e.matches ? 'dark' : 'light');
            }
        });

        // Scroll reveal animation
        const observerOptions = {
            threshold: 0.1,
            rootMargin: '0px 0px -50px 0px'
        };

        const observer = new IntersectionObserver((entries) => {
            entries.forEach(entry => {
                if (entry.isIntersecting) {
                    entry.target.classList.add('revealed');
                }
            });
        }, observerOptions);

        document.querySelectorAll('.scroll-reveal').forEach(el => {
            observer.observe(el);
        });

        // Header scroll effect
        let lastScrollTop = 0;
        const header = document.querySelector('.header');

        window.addEventListener('scroll', () => {
            const scrollTop = window.pageYOffset || document.documentElement.scrollTop;
            
            if (scrollTop > lastScrollTop && scrollTop > 100) {
                header.style.transform = 'translateY(-100%)';
            } else {
                header.style.transform = 'translateY(0)';
            }
            
            lastScrollTop = scrollTop;
        });
    </script>
</body>
</html>